---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('sq-AL', {
			year: 'numeric',
			month: 'long', // 'short' = Shk, 'long' = Shkurt
			day: 'numeric',
		})
	}
</time>